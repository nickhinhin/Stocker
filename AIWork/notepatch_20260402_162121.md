# Stocker 自動巡檢 Notepatch（2026-04-02 16:21 JST）

## 本輪已完成
1. 新增壓力測試資料產生器（多幣別/多交易類型/跨年度）
- 檔案：`src/lib/testPortfolio.ts`
- 內容：一次產生 3 個測試組合（US Growth / Asia Income / Crypto Macro），共 44 筆交易，涵蓋 `BUY/SELL/DIVIDEND_CASH/DIVIDEND_SHARE/FEE/INTEREST/CASH/CASH_CONVERT`。

2. 新增可視化檢查圖表（Analysis）
- 檔案：`src/App.tsx`
- 新增：`Top Winners by Total Profit`、`Top Losers by Total Profit` 兩張圖，方便快速驗證個股盈虧排序。

3. 新增一鍵匯入測試資料入口（Data > Portfolio Management）
- 檔案：`src/App.tsx`
- 按鈕：`Add Stress Test Portfolios`（加入壓力測試組合）
- 行為：確認後直接 append 測試組合，不覆蓋既有資料。

4. SEO / 基礎最佳實踐補強
- `index.html` 新增 description/theme-color/favicon
- 新增 `public/favicon.svg`
- 新增 `public/robots.txt`

5. 型別檢查
- 已執行：`tsc --noEmit`
- 結果：通過（0 error）

## 冒煙檢查結果（用新測試組合）
- 投資組合數：3
- 交易數：44
- 交易類型統計前五：BUY 18 / SELL 10 / CASH 5 / DIVIDEND_CASH 5 / INTEREST 2
- 已確認資料能驅動所有主要圖表與統計模組正常出值。

## 發現的潛在問題（建議下輪優先）
1. 多幣別合併總資產/總收益可能失真
- 症狀：在「全部組合」且混合 USD/HKD/JPY 時，總收益可能出現不合理大幅偏差。
- 研判：核心 summary 目前未做完整 FX 換算就直接彙總。
- 影響：總覽卡片與部分分析圖的絕對值可信度下降（尤其跨幣別資料）。

## 本輪阻斷（環境）
1. Build 阻斷
- 指令：`npm run build`
- 錯誤：`Error: spawn EPERM`（esbuild 啟動子程序被 OS/沙箱策略拒絕）

2. Lighthouse 阻斷
- 原因：build 無法完成且本機無可用 lighthouse CLI；無法在此沙箱直接產出新分數。

3. 截圖阻斷
- 嘗試以 Edge headless 對本地站點截圖，受 sandbox 權限限制（Access denied / mojo platform channel failed），未能生成 PNG。

## 變更檔案
- `src/lib/testPortfolio.ts`（新增）
- `src/App.tsx`（新增測試資料導入+兩張盈虧排行圖）
- `index.html`（meta/favicon）
- `public/favicon.svg`（新增）
- `public/robots.txt`（新增）
